<h1>Posts Page</h1>

<% @posts.each do |post| %>
  <h3><%= post.title %></h3>
  <h3><%= post.author %></h3>
  <h3><%= post.body %></h3>
  <h3><%= link_to 'Post', post_path(post.id) %></h3>
<% end %>

<%= link_to 'New Post', new_post_path %>
